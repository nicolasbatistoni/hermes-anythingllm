name: Release
# Pipeline ÚNICO (reusable central). Ver nicolasbatistoni/.github.
on:
  push:
    branches: [main]
permissions:
  contents: write
  pull-requests: write
  packages: write
jobs:
  pipeline:
    uses: nicolasbatistoni/.github/.github/workflows/release-build.yml@main
    with:
      artifact_type: none
    secrets: inherit
